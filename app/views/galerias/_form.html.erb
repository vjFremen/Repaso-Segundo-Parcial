<%= form_for(@galeria) do |f| %>
  <% if @galeria.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@galeria.errors.count, "error") %> prohibited this galeria from being saved:</h2>

      <ul>
      <% @galeria.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titulo %><br />
    <%= f.text_field :titulo %>
  </div>
	<div class="field">
    	<%= f.label :categoria %><br />
    	<%= f.collection_select :categoria_id, Categoria.all, :id, :titulo %>
	</div>
  <div class="field">
    <%= f.label :descripcion %><br />
    <%= f.text_area :descripcion %>
  </div>
  <div class="field">
    <%= f.label :imagen %><br />
    <%= f.text_field :imagen %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
